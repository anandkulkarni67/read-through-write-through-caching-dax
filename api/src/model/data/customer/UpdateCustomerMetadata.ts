import { CustomerMetadata } from "./CustomerMetadata";
import { VersionedMetadata } from "../VersionedMetadata";

export interface UpdateCustomerMetadata extends CustomerMetadata, VersionedMetadata {

}